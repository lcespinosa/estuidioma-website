<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title or 'Escuela de Idiomas' }}</title>
  <meta name="description" content="{{ seoDescription or 'Clases presenciales y online. Niveles A0–C1.' }}" />
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  {% include "partials/header.njk" %}
  <main>
    {{ content | safe }}
  </main>
  {% include "partials/footer.njk" %}
  <script src="/js/app.js" defer></script>
</body>
</html>
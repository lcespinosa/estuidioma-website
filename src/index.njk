---
layout: layout.njk
title: Escuela de Idiomas
seoDescription: "Clases de idiomas: presencial y online. Niveles A0–C1."
---
{% include "partials/hero.njk" %}
{% include "partials/features.njk" %}

<section id="courses" class="courses">
  <div class="wrap">
    <h2>Cursos</h2>
    <div class="grid cards">
      {% for curso in collections.courses %}
      <article class="card">
        <h3><a href="{{ curso.url }}">{{ curso.data.title }}</a></h3>
        <p class="muted">
          {% if curso.data.level %}Nivel {{ curso.data.level }} • {% endif %}
          {% if curso.data.duration %}{{ curso.data.duration }} • {% endif %}
          {% if curso.data.schedule %}{{ curso.data.schedule }}{% endif %}
        </p>
        {% if curso.data.seoDescription %}<p>{{ curso.data.seoDescription }}</p>{% endif %}
        <p>
          {% if curso.data.price %}<strong>{{ curso.data.price }}</strong>{% endif %}
          <a href="{{ curso.url }}" class="btn">Ver curso</a>
        </p>
      </article>
      {% endfor %}
    </div>
  </div>
</section>

{% include "partials/teachers.njk" %}

{# Busca el markdown con slug "contact" en cualquier colección #}
{% set contact = {} %}
{% for item in collections.all %}
  {% if item.fileSlug == "contact" %}
    {% set contact = item.data %}
  {% endif %}
{% endfor %}

{% include "partials/contact.njk" %}
---
const nav = [
  { href: "/", label: "Inicio" },
  { href: "#courses", label: "Cursos" },
  { href: "#teachers", label: "Profesores" },
  { href: "#contact", label: "Contacto" }
];
---
<header class="site-header">
  <div class="wrap">
    <a class="brand" href="/">Escuela de Idiomas</a>
    <nav class="nav">
      {nav.map((i) => <a href={i.href}>{i.label}</a>)}
    </nav>
  </div>
</header>